# data_science-supervise_learning-regression-regularization-l1-lasso
Lasso regression is a linear regression technique that adds an absolute value penalty (L1 regularization) to the cost function to perform automatic feature selection and prevent overfitting
